# Transfer status

The pull request remains draft. The exact-source CI is authoritative: it must verify the fixed SHA-256 values for the executable workflow and Bridge before this branch can be considered installable.
